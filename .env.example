# --- Supabase ---
# El cliente (navegador) ya tiene valores por defecto en src/lib/config.ts; esto es opcional para el cliente.
PUBLIC_SUPABASE_URL=https://uydwufnirtivbsckiisx.supabase.co

# SECRET KEY: clave TODOPODEROSA (se salta la RLS). SOLO local, NUNCA commitear ni exponer en el cliente.
# Se usa únicamente en scripts de tooling (p. ej. `npm run reset`).
# Dónde sacarla: Supabase -> Project Settings -> API -> "Secret keys" (empieza por sb_secret_...)
SUPABASE_SECRET_KEY=pega-aqui-tu-secret-key
